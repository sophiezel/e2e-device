/**
 * Runtime session management
 */

export function resetRuntimeOverrides(): void {
	// Reset any runtime overrides applied during test execution
	// This is a hook for future runtime management
}
