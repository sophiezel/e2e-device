#!/usr/bin/env bash
# DEPRECATED: This file is kept for backward compatibility.
# All helpers have moved to scripts/lib/common.sh
# Source this file to get the canonical helpers.
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/lib/common.sh"
